on:
  schedule:
    - cron: "30 * * * *"  # Runs every hour at the 30th minute

jobs:
  daily-task:
    runs-on: ubuntu-latest

    steps:
      - name: Print a daily message
        run: echo "Running the daily scheduled task!"